<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cis490.alex.notmymusic" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MusicIndexActivity"
            android:label="@string/title_activity_music_index" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".MusicDetailActivity"
            android:label="@string/title_activity_music_detail" >
        </activity>
        <activity
            android:name=".AddMusicActivity"
            android:label="@string/title_activity_add_music" >
        </activity>
    </application>

</manifest>
